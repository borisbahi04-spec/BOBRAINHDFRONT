
export const authGuard = (handler: (arg0: any, arg1: any) => any) => {
  return async (req: any, res: any) => {
    // Check if the user is authenticated here
    const isAuthenticated = true/* Your authentication logic here */;


    if (!isAuthenticated) {

      return {
        redirect: {
          destination: '/login',
          permanent: false,
        },
      }
    }

    return handler(req, res);
  };
};
